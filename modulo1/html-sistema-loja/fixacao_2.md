```function calculaPrecoTotal(quantidade) {
  // Escreva seu código aqui
  if (quantidade >=12) {
      let preco = 1
      return quantidade * preco
  } else {
      preco = 1.30
      return quantidade * preco
  }
}
